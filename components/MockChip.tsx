/** Tiny amber pill. Never let a judge think a mock is real. */
export default function MockChip() {
  return (
    <span className="rounded-full border border-rule px-2.5 py-0.5 text-[11px] font-medium text-muted">
      MOCK
    </span>
  );
}
