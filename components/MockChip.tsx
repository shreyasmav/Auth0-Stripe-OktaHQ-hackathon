/** Tiny amber pill. Never let a judge think a mock is real. */
export default function MockChip() {
  return (
    <span className="inline-flex items-center rounded-full border border-warn/50 bg-warn/10 px-2 py-0.5 font-mono text-[11px] font-semibold tracking-widest text-warn">
      MOCK
    </span>
  );
}
